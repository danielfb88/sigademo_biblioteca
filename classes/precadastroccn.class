<?php
/* Copyright 2006, 2007, 2008, 2009, 2010 do SETEC/MEC
 *
 * Este arquivo é parte do programa SigaEPT
 *
 * O SigaEPT é um software livre;  você pode redistribuí-lo e/ou modificá-lo dentro dos
 * termos da Licença Pública Geral GNU como publicada pela fundação do software livre (FSF);
 * na versão 2 da Licença.
 *
 * Este programa é distribuído na esperança que possa ser útil, mas SEM NENHUMA GARANTIA; sem
 * uma garantia implícita de ADEQUAÇÃO a qualquer MERCADO ou APLICAÇÃO EM PARTICULAR. Veja Licença
 * Pública Geral GNU/GPL em português para maiores detalhes.
 *
 * Você deve ter recebido uma cópia da Licença Pública Geral GNU, sob o título “LICENCA.txt”,
 * junto com este programa, se não, acesse o portal do Software Público Brasileiro no endereço
 * www.softwarepublico.gov.br ou escreva para Fundação do Software Livre (FSF) Inc.,51 Franklin
 * St, Fifth Floor, Boston, MA 02110-1301, USA
 */
?>
<?php
/**
* @package SIGA
* @subpackage Biblioteca
* @author SETEC/MEC
*/

class BusinessBibliotecaPreCadastroCCN extends Business
{

	var $issn;
	var $titulo;
	var $codccn;
	var $usuario;

	function __construct($data = null)
	{
       $this->GetDatabase('ccn');
       if ($data)
          $this->SetData($data);
	}

	function GetData()
	{
		$data = new FormData();
		$data->issn = $this->issn;
		$data->titulo = $this->titulo;
        $data->codccn = $this->codccn;
		$data->usuario = $this->usuario;
		return $data;
	}


	function SetData($data)
	{
		$this->issn = $data->issn;
		$this->titulo = $data->titulo;
		$this->codccn = $data->codccn;
		$this->usuario = $data->usuario;
	}

	function GetByTitulo($titulo)
	{
		
	}

	function Update($data)
	{
	
	}

	
	function Insert($data)
	{
		
		$sql = new sql('titulo,issn,codccn,usuario,data_hora_insercao','pre_cadastro_ccn');

		//$novo_codigo = $this->_db->GetNewId('seq_gtc_grupo');

		$values = array($data->titulo,$data->issn,$data->codccn,$data->usuario,'now');
		
		$ok = $this->Execute($sql->Insert($values));

		return $ok;

	}

	
	function Delete($data)
	{
		
		$sql = new sql('','pre_cadastro_ccn','issn = ?');

		$ok = $this->Execute($sql->Delete($data->issn));

		return $ok;

	}

	function ListPreCadastro()
	{
		$sql = new sql('titulo,issn,codccn,usuario,data_hora_insercao','pre_cadastro_ccn','','titulo');
        $query = $this->Query($sql);
        return $query;
	}

}
?>
