<?xml version="1.0" standalone="yes"?>
<conf>
    <login>
        <check>0</check>
    </login>
    <mail>
        <host>smtp.gmail.com</host> <!-- Servidor SMTP -->
        <ssl>TRUE</ssl>
        <auth>TRUE</auth> <!-- FALSE, não requer autenticação - TRUE, requer autenticação -->
                <user>siga.adm@ifms.edu.br</user> <!-- Usuário do servidor SMTP, se servidor requer autenticação -->
                <pass>zJkacBcVh</pass> <!-- Senha do servidor SMTP, se servidor requer autenticação -->
                <from>siga.adm@ifms.edu.br</from> <!-- E-mail utilizado para o envio -->
        <port>465</port> <!-- Geralmente 25 se servidor não requer autenticação, 587 caso contrário -->
    </mail>	
</conf>

